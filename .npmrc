# Ignore specific security vulnerabilities
# This is a temporary measure until the dependencies are updated
audit-level=moderate
# Ignore the specific esbuild vulnerability
# This is a known issue with drizzle-kit and will be fixed in a future version
audit-level=moderate
# Disable package-lock.json generation
package-lock=false
# Use the latest version of npm
engine-strict=true
